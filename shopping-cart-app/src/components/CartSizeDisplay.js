function CartSizeDisplay({cartSize}) {
    return (
        // TODO: control below text in curly brackets via state later
        <div className="CartSizeDisplay">
            There are {cartSize} items in the cart!
        </div>
    );
}

export default CartSizeDisplay;
